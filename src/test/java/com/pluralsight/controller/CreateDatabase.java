package com.pluralsight.controller;

public class CreateDatabase {
	
	
	/*
	 * create table ride_tracker.ride ( id int not null auto_increment, name
	 * varchar(100) NOT NULL, duration int not null, primary key(id));
	 * 
	 * ALTER TABLE ride ADD ride_date DATETIME AFTER duration;
	 * 
	 */

}
